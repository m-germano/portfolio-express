<section class="text-white px-4 sm:px-6 py-20" id="contato">
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold bg-gradient-to-r from-purpleAccent to-pink-500 bg-clip-text text-transparent mb-4">
        Entre em Contato
      </h2>
      <p class="text-gray-400 max-w-2xl mx-auto">
        Tem alguma pergunta ou quer trabalhar juntos? Envie-me uma mensagem e responderei o mais breve possível.
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
      <!-- Formulário -->
      <form id="contact-form" method="POST" action="/messages" class="space-y-6 bg-darker/50 p-8 rounded-2xl border border-darkBorder backdrop-blur-sm">
        <div class="space-y-4">
          <div class="relative">
            <input
              type="text"
              name="name"
              placeholder="Seu Nome"
              class="w-full px-5 py-3 bg-[#1a1a1a] border border-darkBorder rounded-xl focus:outline-none focus:ring-2 focus:ring-purpleAccent focus:border-transparent transition-all duration-300 placeholder-gray-500"
              required
            />
           
          </div>

          <div class="relative">
            <input
              type="email"
              name="email"
              placeholder="Seu Email"
              class="w-full px-5 py-3 bg-[#1a1a1a] border border-darkBorder rounded-xl focus:outline-none focus:ring-2 focus:ring-purpleAccent focus:border-transparent transition-all duration-300 placeholder-gray-500"
              required
            />
          </div>

          <div class="relative">
            <input
              type="text"
              name="subject"
              placeholder="Assunto"
              class="w-full px-5 py-3 bg-[#1a1a1a] border border-darkBorder rounded-xl focus:outline-none focus:ring-2 focus:ring-purpleAccent focus:border-transparent transition-all duration-300 placeholder-gray-500"
              required
            />
           
          </div>

          <div class="relative">
            <textarea
              name="message"
              rows="6"
              placeholder="Sua Mensagem"
              class="w-full px-5 py-3 bg-[#1a1a1a] border border-darkBorder rounded-xl focus:outline-none focus:ring-2 focus:ring-purpleAccent focus:border-transparent transition-all duration-300 placeholder-gray-500 resize-none"
              required
            ></textarea>
          
          </div>
        </div>

        <button
          type="submit"
          class="w-full bg-gradient-to-r from-purpleAccent to-pink-500 hover:from-purple-600 hover:to-pink-600 text-white font-medium py-3 px-6 rounded-xl transition-all duration-300 flex items-center justify-center space-x-2 shadow-lg hover:shadow-purpleAccent/20 transform hover:-translate-y-0.5"
        >
          <i class="fas fa-paper-plane mr-2"></i>
          <span>Enviar Mensagem</span>
        </button>
      </form>

      <!-- Informações de contato -->
      <div class="space-y-8 bg-darker/50 p-8 rounded-2xl border border-darkBorder backdrop-blur-sm h-full">
        <div>
          <h3 class="text-2xl font-bold text-white mb-6 flex items-center">
            <i class="fas fa-info-circle text-purpleAccent mr-3"></i>
            Informações de Contato
          </h3>
          
          <div class="space-y-5">
            <div class="flex items-start space-x-4">
              <div class="bg-[#1a1a1a] p-3 rounded-lg text-purpleAccent">
                <i class="fas fa-map-marker-alt text-lg"></i>
              </div>
              <div>
                <h4 class="font-medium text-gray-300">Localização</h4>
                <p class="text-gray-400">São José dos Campos - SP</p>
              </div>
            </div>

            <div class="flex items-start space-x-4">
              <div class="bg-[#1a1a1a] p-3 rounded-lg text-purpleAccent">
                <i class="fas fa-phone text-lg"></i>
              </div>
              <div>
                <h4 class="font-medium text-gray-300">Telefone/WhatsApp</h4>
                <a href="https://wa.me/5512981165109" class="text-gray-400 hover:text-purpleAccent transition-colors">(12) 98116-5109</a>
              </div>
            </div>

            <div class="flex items-start space-x-4">
              <div class="bg-[#1a1a1a] p-3 rounded-lg text-purpleAccent">
                <i class="fas fa-envelope text-lg"></i>
              </div>
              <div>
                <h4 class="font-medium text-gray-300">Email</h4>
                <a href="mailto:silvaa.matheus112@gmail.com" class="text-gray-400 hover:text-purpleAccent transition-colors">silvaa.matheus112@gmail.com</a>
              </div>
            </div>
          </div>
        </div>

        <div>
          <h3 class="text-2xl font-bold text-white mb-6 flex items-center">
            <i class="fas fa-share-alt text-purpleAccent mr-3"></i>
            Redes Sociais
          </h3>
          
          <div class="flex space-x-4">
            <a href="https://github.com/m-germano" target="_blank" class="bg-[#1a1a1a] hover:bg-[#2d2d2d] p-4 rounded-xl text-white transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg hover:shadow-purpleAccent/10" title="GitHub">
              <i class="fab fa-github text-xl"></i>
            </a>
            <a href="https://www.instagram.com/m.germano._" target="_blank" class="bg-[#1a1a1a] hover:bg-[#2d2d2d] p-4 rounded-xl text-white transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg hover:shadow-purpleAccent/10" title="Instagram">
              <i class="fab fa-instagram text-xl"></i>
            </a>
            <a href="https://wa.me/5512981165109" target="_blank" class="bg-[#1a1a1a] hover:bg-[#2d2d2d] p-4 rounded-xl text-white transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg hover:shadow-purpleAccent/10" title="WhatsApp">
              <i class="fab fa-whatsapp text-xl"></i>
            </a>
            <a href="https://linkedin.com/in/math-germano" target="_blank" class="bg-[#1a1a1a] hover:bg-[#2d2d2d] p-4 rounded-xl text-white transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg hover:shadow-purpleAccent/10" title="LinkedIn">
              <i class="fab fa-linkedin text-xl"></i>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Modal de Sucesso -->
  <div id="modal-success" class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/80 backdrop-blur-sm opacity-0 pointer-events-none transition-opacity duration-300">
    <div class="bg-darker border border-darkBorder rounded-2xl p-8 max-w-md w-full transform transition-all duration-300 scale-95">
      <div class="text-center">
        <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-green-500/20 mb-6">
          <i class="fas fa-check text-green-400 text-2xl"></i>
        </div>
        <h3 class="text-2xl font-bold text-white mb-3">Mensagem Enviada!</h3>
        <p class="text-gray-400 mb-6">Sua mensagem foi enviada com sucesso. Responderei o mais breve possível.</p>
        <button onclick="closeModal('success')" class="w-full bg-gradient-to-r from-purpleAccent to-pink-500 text-white font-medium py-3 px-6 rounded-xl hover:opacity-90 transition-opacity duration-300">
          Fechar
        </button>
      </div>
    </div>
  </div>

  <!-- Modal de Erro -->
  <div id="modal-error" class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/80 backdrop-blur-sm opacity-0 pointer-events-none transition-opacity duration-300">
    <div class="bg-darker border border-darkBorder rounded-2xl p-8 max-w-md w-full transform transition-all duration-300 scale-95">
      <div class="text-center">
        <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-red-500/20 mb-6">
          <i class="fas fa-times text-red-400 text-2xl"></i>
        </div>
        <h3 class="text-2xl font-bold text-white mb-3">Ocorreu um erro!</h3>
        <p class="text-gray-400 mb-6">Não foi possível enviar sua mensagem. Por favor, tente novamente mais tarde.</p>
        <button onclick="closeModal('error')" class="w-full bg-gradient-to-r from-purpleAccent to-pink-500 text-white font-medium py-3 px-6 rounded-xl hover:opacity-90 transition-opacity duration-300">
          Tentar Novamente
        </button>
      </div>
    </div>
  </div>
</section>

<script>
  const form = document.getElementById("contact-form");

  form.addEventListener("submit", async (event) => {
    event.preventDefault();
    
    // Mostrar estado de carregamento
    const submitBtn = form.querySelector('button[type="submit"]');
    const originalBtnText = submitBtn.innerHTML;
    submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i> Enviando...';
    submitBtn.disabled = true;

    const formData = {
      name: form.name.value,
      email: form.email.value,
      subject: form.subject.value,
      message: form.message.value,
    };

    try {
      const response = await fetch("/messages", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify(formData),
      });

      const result = await response.json();

      if (result.success) {
        showModal("success");
        form.reset(); // Limpa o formulário após envio bem-sucedido
      } else {
        showModal("error");
      }
    } catch (error) {
      console.error("Erro ao enviar:", error);
      showModal("error");
    } finally {
      // Restaurar botão
      submitBtn.innerHTML = originalBtnText;
      submitBtn.disabled = false;
    }
  });

  function showModal(type) {
    const modal = document.getElementById(`modal-${type}`);
    modal.classList.remove("opacity-0", "pointer-events-none", "scale-95");
    modal.classList.add("opacity-100", "pointer-events-auto", "scale-100");
  }

  function closeModal(type) {
    const modal = document.getElementById(`modal-${type}`);
    modal.classList.remove("opacity-100", "pointer-events-auto", "scale-100");
    modal.classList.add("opacity-0", "pointer-events-none", "scale-95");
  }
</script>