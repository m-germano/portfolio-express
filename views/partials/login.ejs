<!DOCTYPE html>
<html lang="pt-BR" class="h-full">
  <head>
    <meta charset="UTF-8" />
    <title>Login | Meu Portfólio</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              dark: "#0f0f0f",
              darker: "#0a0a0a",
              darkBorder: "#252525",
              purpleAccent: "#AD46FF",
            },
          },
        },
      };
    </script>
  </head>

  <body class="bg-dark text-white min-h-screen flex flex-col">
    <%- include('../partials/header') %>

    <main
      class="flex-grow flex items-center justify-center pt-20 pb-20 px-4 sm:px-6 lg:px-8"
    >
      <div class="w-full max-w-md space-y-10">
        <div class="text-center">
          <div
            class="mx-auto bg-gradient-to-r from-purple-600 to-pink-500 w-20 h-20 rounded-full flex items-center justify-center mb-6"
          >
            <i class="fas fa-sign-in-alt text-3xl text-white"></i>
          </div>
          <h2
            class="text-4xl font-bold bg-gradient-to-r from-purple-500 to-pink-500 bg-clip-text text-transparent"
          >
            Faça login
          </h2>
          <p class="mt-4 text-base text-gray-400">
            Ou
            <a
              href="/register"
              class="font-medium text-purpleAccent hover:text-purple-300 transition-colors duration-300"
            >
              crie uma nova conta
            </a>
          </p>
        </div>

        <form class="mt-10 space-y-8" action="/login" method="POST">
          <div class="space-y-6">
            <div class="relative">
              <div
                class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none"
              >
                <i class="fas fa-user text-gray-400 text-lg"></i>
              </div>
              <input
                id="email"
                name="email"
                type="text"
                required
                class="block w-full pl-12 pr-4 py-4 bg-[#1a1a1a] border border-darkBorder rounded-xl placeholder-gray-400 text-white focus:outline-none focus:ring-2 focus:ring-purpleAccent focus:border-transparent transition-all duration-300"
                placeholder="Email ou nome de usuário"
              />
            </div>

            <div class="relative">
              <div
                class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none"
              >
                <i class="fas fa-lock text-gray-400 text-lg"></i>
              </div>
              <input
                id="password"
                name="password"
                type="password"
                required
                class="block w-full pl-12 pr-4 py-4 bg-[#1a1a1a] border border-darkBorder rounded-xl placeholder-gray-400 text-white focus:outline-none focus:ring-2 focus:ring-purpleAccent focus:border-transparent transition-all duration-300"
                placeholder="Senha"
              />
            </div>
          </div>

          <div class="flex items-center justify-between mt-6">
            <div class="flex items-center">
              <input
                id="remember-me"
                name="remember-me"
                type="checkbox"
                class="h-5 w-5 text-purpleAccent focus:ring-purpleAccent border-darkBorder rounded bg-[#1a1a1a]"
              />
              <label for="remember-me" class="ml-3 block text-sm text-gray-400">
                Lembrar de mim
              </label>
            </div>
            <div class="text-sm">
              <a
                href="#"
                class="font-medium text-purpleAccent hover:text-purple-300 transition-colors duration-300"
              >
                Esqueceu sua senha?
              </a>
            </div>
          </div>

          <div class="mt-8">
            <button
              type="submit"
              class="group relative w-full flex justify-center py-4 px-4 border border-transparent text-base font-medium rounded-xl text-white bg-gradient-to-r from-purpleAccent to-purple-600 hover:opacity-90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purpleAccent transition-all duration-300 shadow-lg hover:shadow-purpleAccent/30 transform hover:-translate-y-0.5"
            >
              <span class="absolute left-0 inset-y-0 flex items-center pl-5">
                <i class="fas fa-sign-in-alt text-purple-200 text-lg"></i>
              </span>
              Entrar
            </button>
          </div>
        </form>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
